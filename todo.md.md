update init screen to also ask for credentials username and password, if all 3 are validated, and API resonnds with a jwt key store it and take user to home screen. Remeber api url and username that user entered, but not the passowrd, if authentication was success then store the JWT token as well, as it is required to make ALL subsequent requests. If and when JWT expires (yuo know this if resoonse is 401/403,) then present this screen to the user again, with URL and username prepopulated, they only need to enter the password to get new token. Proactively refresh token if its about to expire , by calling appropriate API method and store latest JWT